# Plan: Stage and Commit Backoffice Changes

## Goal

Stage and commit the backoffice lint fixes that resulted from running `eslint --fix`, then push to remote.

## Changes Overview

20 files modified — all are lint/import-order fixes from running `eslint --fix` during the previous commit:

- Import order fixes (empty lines between groups)
- Proper ordering of external/internal imports
- Prettier formatting applied via lint-staged

## Steps

1. **Stage backoffice changes**

   ```bash
   git add applications/backoffice/
   ```

2. **Review staged changes**
   - Verify all 20 files are staged
   - Confirm they're all lint fixes

3. **Commit with conventional message**

   ```
   style(backoffice): apply lint fixes to resolve import order violations

   - Add empty lines between import groups
   - Fix import ordering (external before internal)
   - Applied via eslint --fix
   ```

4. **Push to remote**
   ```bash
   git push
   ```

## Notes

- These are auto-generated lint fixes from the pre-commit hook
- No functional changes — only formatting/import order
